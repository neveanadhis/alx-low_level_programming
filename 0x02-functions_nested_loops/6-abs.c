#include "main.h"
/**
 * _abs - function that computes the absolute value
 *@r: the value
 * Description: the standard library provides a similar
 *
 * Return: always 0
 */
int _abs(int r)
{
	if (r >= 0)

		return (r);
	else
		return (r * -1);
}
